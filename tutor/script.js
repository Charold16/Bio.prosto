const menuBtn=document.getElementById('menuBtn');const navLinks=document.getElementById('navLinks');menuBtn?.addEventListener('click',()=>{const open=navLinks.classList.toggle('open');menuBtn.setAttribute('aria-expanded',open?'true':'false');menuBtn.textContent=open?'×':'☰'});navLinks?.querySelectorAll('a').forEach(link=>link.addEventListener('click',()=>{navLinks.classList.remove('open');menuBtn.setAttribute('aria-expanded','false');menuBtn.textContent='☰'}));document.querySelectorAll('.faq').forEach(item=>item.querySelector('button')?.addEventListener('click',()=>item.classList.toggle('open')));const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting)entry.target.classList.add('visible')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));const FORM_ENDPOINT='PASTE_GOOGLE_APPS_SCRIPT_WEB_APP_URL_HERE';const contactForm=document.getElementById('contactForm');const formNote=document.getElementById('formNote');const submitBtn=document.getElementById('submitBtn');function setStatus(text,type=''){formNote.textContent=text;formNote.className=`form-note ${type}`.trim()}contactForm?.addEventListener('submit',async event=>{event.preventDefault();setStatus('');if(!contactForm.checkValidity()){contactForm.reportValidity();setStatus('Заполните обязательные поля.','error');return}const data=new FormData(contactForm);if(data.get('website'))return;if(FORM_ENDPOINT.includes('PASTE_GOOGLE_APPS_SCRIPT')){setStatus('Форма пока не подключена к Google Таблице.','error');return}data.append('submittedAt',new Date().toISOString());data.append('pageUrl',location.href);submitBtn.disabled=true;submitBtn.textContent='Отправляем…';try{await fetch(FORM_ENDPOINT,{method:'POST',mode:'no-cors',body:data});contactForm.reset();setStatus('Заявка отправлена. Я свяжусь с вами, чтобы согласовать время диагностики.','success');submitBtn.textContent='Заявка отправлена ✓'}catch(e){setStatus('Не удалось отправить заявку. Попробуйте ещё раз.','error');submitBtn.textContent='Повторить отправку'}finally{submitBtn.disabled=false}});
